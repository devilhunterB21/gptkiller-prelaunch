import "./globals.css";

export const metadata = {
  title: "GPTKILLER — Pre-Launch",
  description: "I am not here to join their game. I am here to own the board.",
};

export default function RootLayout({ children }) {
  return (<html lang="en"><body>{children}</body></html>);
}
